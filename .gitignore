# secrets — never commit
.env
.env.*
!.env.example
*.key
key.json

# build
out/
cache/
node_modules/
broadcast/

# fixtures may contain large payloads but ARE committed on purpose
!evidence/**/fixtures/
